# ============================================================================
# FULLY-PINNED LOCKFILE — every package AND sub-dependency at an exact version.
# ----------------------------------------------------------------------------
# Why: this file used to pin only the ~22 DIRECT libraries and let their
# sub-dependencies float. A routine redeploy then rebuilt the image and pulled
# a newer `dnspython` (2.8.0) that eventlet 0.33.3's greendns resolver can't
# drive — every public DNS lookup began timing out and ALL outbound SMS went
# silently dark the day after Treenet. Pinning the whole set means a rebuild
# installs a byte-identical environment and can NEVER silently drift again.
#
# Generated from `pip freeze` on the working production `web` container
# (python:3.11-slim) 2026-09-05. To change a dependency: edit it here, deploy,
# then re-freeze from the container and paste the result back.
# ============================================================================

aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiohttp-retry==2.9.1
aiosignal==1.4.0
APScheduler==3.10.4
attrs==26.1.0
bidict==0.24.1
bleach==6.1.0
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
Deprecated==1.3.1
# dnspython drives eventlet's greendns resolver. greendns is DISABLED in
# run_server.py (EVENTLET_NO_GREENDNS=yes), so this version is no longer
# DNS-critical — but it is pinned here so a rebuild can't reintroduce the
# 2.8.0-vs-eventlet-0.33.3 incompatibility that took SMS down.
dnspython==2.8.0
ecdsa==0.19.2
# eventlet is monkey-patched at boot (run_server.py) for request concurrency
# (#546). Pinned; do not bump without re-testing DNS + SocketIO.
eventlet==0.33.3
Flask==2.3.3
Flask-Cors==4.0.0
Flask-JWT-Extended==4.5.3
Flask-Limiter==3.3.1
Flask-SocketIO==5.3.6
frozenlist==1.8.0
greenlet==3.5.5
gunicorn==21.2.0
h11==0.16.0
idna==3.19
itsdangerous==2.2.0
Jinja2==3.1.6
limits==5.8.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
multidict==6.7.1
ordered-set==4.1.0
packaging==26.3
passlib==1.7.4
# Pillow is REQUIRED at runtime: qrcode only declares it under its optional
# [pil] extra, so a bare qrcode install does NOT pull it. Without Pillow every
# QR render (SMS order QR, customer receipt QR, thermal labels) raises
# ImportError in production. Pin it explicitly.
Pillow==10.1.0
propcache==0.5.2
# psutil powers the Support -> Health "Application Host (CPU/Memory)" tile.
# It was once imported top-level but undeclared, so on Railway the import
# failed and the ENTIRE support_api blueprint silently failed to register.
# Real runtime dep — keep it pinned.
psutil==5.9.5
psycogreen==1.0.2
psycopg2-binary==2.9.9
pyasn1==0.6.4
Pygments==2.21.0
PyJWT==2.13.0
pypng==0.20220715.0
python-dotenv==1.0.0
python-engineio==4.14.0
python-jose==3.3.0
python-magic==0.4.27
python-socketio==5.16.4
pytz==2026.3.post1
qrcode==7.4.2
redis==5.0.1
requests==2.31.0
rich==13.9.4
rsa==4.9.1
simple-websocket==1.1.0
six==1.17.0
SQLAlchemy==2.0.21
twilio==8.5.0
typing_extensions==4.16.0
tzlocal==5.4.4
urllib3==2.7.0
webencodings==0.6.1
Werkzeug==3.1.8
wrapt==2.4.0
wsproto==1.3.2
yarl==1.24.5
