# Essential dependencies only
eventlet==0.33.3
flask==2.3.3
flask-cors==4.0.0
flask-jwt-extended==4.5.3
flask-socketio==5.3.6
flask-limiter==3.3.1
psycopg2-binary==2.9.9
psycogreen==1.0.2
sqlalchemy==2.0.21
python-dotenv==1.0.0
passlib==1.7.4
bleach==6.1.0
redis==5.0.1
requests==2.31.0
gunicorn==21.2.0
twilio==8.5.0
qrcode==7.4.2
# Pillow is REQUIRED at runtime: qrcode only declares it under its
# optional [pil] extra, so a bare `qrcode==7.4.2` install does NOT pull
# it. Without Pillow every QR render (SMS order QR, customer receipt QR,
# thermal labels) raises ImportError in production. Pin it explicitly.
Pillow==10.1.0
python-magic==0.4.27
APScheduler==3.10.4
python-jose==3.3.0
# psutil powers the Support → Health "Application Host (CPU/Memory)" tile
# (routes/support_api_routes.py). It was imported at module top-level but
# never declared here, so on Railway the import failed and the ENTIRE
# support_api blueprint silently failed to register — every /api/diagnostics/*,
# /api/emergency/*, and the Comms Hub broadcast route 404'd. The import is now
# also lazy (see support_api_routes.py) so a future missing dep can't repeat
# this, but psutil is a real runtime dep for the perf tile, so pin it.
psutil==5.9.5